import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yumrate.app',
  appName: 'Yumrate',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
